import { ReportUploadingFormValidationDirective } from './report-uploading-form-validation.directive';

describe('ReportUploadingFormValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new ReportUploadingFormValidationDirective();
    expect(directive).toBeTruthy();
  });
});
