import { ValidPasswordDirective } from './valid-password.directive';

describe('ValidPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
